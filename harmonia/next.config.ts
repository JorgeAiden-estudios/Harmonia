import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      /*.supabase.co"   // FALTA POR AÑADIR, HORA DEL VIDEO 3HORAS Y 10 MIN*/ 
    ]
   } /* config options here */
};

export default nextConfig;

